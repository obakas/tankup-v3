# Prompt 04 — Frontend MVP Shell

Read:
- `docs/workflow.md`
- `docs/statuses.md`
- `docs/mvp-scope.md`
- `frontend/src/App.tsx`

Task:
Replace the Vite starter screen with a clean TankUp V3 MVP operational shell.

Requirements:
- Use React + TypeScript.
- Keep it simple; no heavy UI library.
- Create a layout with:
  - header,
  - sidebar or top nav,
  - main operational panel,
  - delivery status timeline placeholder,
  - dev/testing notice.
- Show current MVP focus:
  - delivery workflow,
  - status integrity,
  - event/audit visibility.
- Do not implement fake production auth.
- Do not invent customer payment flow yet.
- Keep styling in existing CSS or simple new CSS files.

After changes, run:

```bash
cd frontend
npm run build
```

Report changed files and build result.
